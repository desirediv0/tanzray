import localFont from "next/font/local";
import "./globals.css";
import Navbar from "@/components/Navbar";
import Footer from "@/components/Footer";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
  weight: "100 900",
});
const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
  weight: "100 900",
});

export const metadata = {
  title: "TANZRAY LEGAL ATTORNEYS | Navigate the Legal Mosaic",
  description: "Premium legal services by Advocate Manjeet Kaur & Associates. Law firm in Delhi specializing in Matrimonial, Criminal, Contract, Mediation, and Intellectual Property law.",
  keywords: "Law Firm Delhi, Advocate, Legal Services, Matrimonial Law, Criminal Law, Contract Law, Mediation",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-white text-gray-900`}
      >
        <Navbar />
        <main className="min-h-screen pt-16">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
}
